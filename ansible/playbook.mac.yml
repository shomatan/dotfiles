- hosts: localhost
  connection: local
  gather_facts: no
  roles:
    - homebrew
    - macos
    - dotfiles
    - asdf
    - vscode
  vars:
    vscode_extensions:
      - ms-vscode-remote.vscode-remote-extensionpack
